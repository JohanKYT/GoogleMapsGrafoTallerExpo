<dependencies>
    <!-- JavaFX Controls -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-controls</artifactId>
        <version>21.0.0</version> <!-- Última versión estable -->
    </dependency>
    <!-- JavaFX Web -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-web</artifactId>
        <version>21.0.0</version>
    </dependency>
</dependencies>

<build>
<plugins>
    <!-- Plugin para configurar el runtime de JavaFX -->
    <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <executions>
            <execution>
                <id>default-cli</id>
                <goals>
                    <goal>run</goal>
                </goals>
            </execution>
        </executions>
        <configuration>
            <mainClass>TuClasePrincipal</mainClass>
        </configuration>
    </plugin>
</plugins>
</build>


